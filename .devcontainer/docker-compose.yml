version: '3.8'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspaces:cached
    # 保持容器一直运行
    command: sleep infinity
    # 如需加入 DB/Redis 等服务，可在此文件新增对应服务并用默认网络
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_ADMIN
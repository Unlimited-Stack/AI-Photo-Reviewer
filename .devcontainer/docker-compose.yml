version: '3.8'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace:cached
    # 保持容器一直运行
    command: /bin/sh -c "while sleep 1000; do :; done"
    # 预留给后续可能加入的 db 服务等
    network_mode: service:app

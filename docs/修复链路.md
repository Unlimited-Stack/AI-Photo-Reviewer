babel-preset-expo (在 /workspaces/node_modules/)
  └─ hasModule('expo-router')
       └─ require.resolve('expo-router')  ← 从根 node_modules 出发
            └─ ❌ 找不到（expo-router 在 apps/native/node_modules/，未被 hoist）
                 └─ expoRouterBabelPlugin 不加入
                      └─ process.env.EXPO_ROUTER_APP_ROOT 不被替换
                           └─ Metro 报错: Invalid call

修复: babel.config.js 中用 require.resolve('expo-router', { paths: [__dirname] })
      从 apps/native/ 上下文检测，确认存在后手动加入 expoRouterBabelPlugin
      → process.env.EXPO_ROUTER_APP_ROOT 正确替换为相对路径
      → require.context('../../app', ...) 正确找到路由
      → 界面正常渲染 ✅